import theme from 'theme';

const styles = {
  main: {
    display: 'flex',
  },
  minMax: {
    padding: theme.freeSpace,
  },
  addPoints: {
    padding: theme.freeSpace,
    flexGrow: 1,
    marginLeft: theme.freeSpace,
  },
};

export default styles;
